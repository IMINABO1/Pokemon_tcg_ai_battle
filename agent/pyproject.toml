[project]
name = "ptcg-agent"
version = "0.1.0"
description = "PTCG AI Battle PIMC Search Agent"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
